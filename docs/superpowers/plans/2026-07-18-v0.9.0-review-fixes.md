# v0.9.0 External Review Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 4巡目外部レビュー（P1×6 / P2×8 / P3×6、全件コード照合済み）を修正し、v0.9.0 としてリリースする。

**Architecture:** hook subsystem の安全境界（containment 判定・managed block 状態機械・挿入位置・結果型）を作り直し、prune/deinit/doctor の契約違反を修正、CLI 表面（flag 排他・LABEL 列・JSON envelope）を整え、lib target を廃止して binary-only にする。ID は 32 hex（128bit）へ拡張（破壊的変更、ユーザー承認済み）。

**Tech Stack:** Rust (edition 2021), clap 4, serde/serde_json, sha2, tempfile。テストは unit（各モジュール内）+ CLI E2E（tests/cli.rs、隔離 HOME/XDG）。

## Global Constraints

- ユーザー決定事項: **lib target 廃止（binary-only）** / **ID は 32hex に拡張（破壊的）** / **backup は doc 弱め＋check 検出** / **性能は overlap validation の sort 化のみ（ベンチ・lock 見直しはしない）**
- ブロック値を exact assert する新規テストは専用ポートレンジを使う。使用済み: 3000s/3800s/3900s/4200-4319, 17300-17699, 18000-18299, 18500-18599, 18600-18609, 18700-18799, 18800-18809, 18900-18959, 19100-19199, 19200-19299, 19300-19399。**新規タスクは 19400 以降を 100 刻みで確保し、着手時に grep で空きを確認**。
- 各タスク完了時に `cargo fmt` + `cargo clippy --all-targets -- -D warnings` + `cargo test` を通してからコミット（実行は sonnet subagent へ委譲可）。
- コミットは新規コミットのみ（`--amend` 禁止）。メッセージ末尾に `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- ブランチ: `feature/v0.9.0-review-fixes`（main から作成）。PR は main 向け。
- タスク順は依存を含む: Task 1〜3 は hook 書き込み経路を共有するので順番厳守。Task 14（ID 32hex）は Task 15（binary-only）より先。

---

### Task 1: core.hooksPath の containment 判定（指摘1）

**Files:**
- Modify: `src/hooks.rs` (resolve: 84-106, 新ヘルパー追加)
- Test: `src/hooks.rs` 内 unit tests + `tests/cli.rs` E2E

**Interfaces:**
- Produces: `HooksLocation::resolve(ctx)` の新セマンティクス — 解決先（canonicalize 後）がリポジトリ（worktree_root / common_dir 配下）の外なら **scope を問わず** `SharedScope` として拒否。`classify` は純関数のまま。
- 内部ヘルパー `fn lexical_normalize(path: &Path) -> PathBuf`（`..`/`.` をテキスト解決; canonicalize 不能時のフォールバック）。

**設計:** 旧実装は「絶対パス × global/system scope」だけ拒否していた。新実装は「解決先がリポジトリ外」を一律拒否に変更する（`command` scope も相対 `../` 脱出も symlink 脱出もこれで閉じる）。scope はエラーメッセージ用の情報としてだけ取得する。リポジトリ内に解決されるパスは従来どおり classify（Husky/Custom/Missing）。

- [ ] **Step 1: 失敗するテストを書く**

`src/hooks.rs` の tests に（`resolve` は GitCtx が要るので、判定ロジックを純関数 `fn is_inside_repo(resolved: &Path, worktree_root: &Path, common_dir: &Path) -> bool` に切り出してテスト）:

```rust
#[test]
fn relative_hooks_path_escaping_repo_is_outside() {
    let tmp = tempfile::TempDir::new().unwrap();
    let root = tmp.path().join("repo");
    std::fs::create_dir_all(root.join(".git")).unwrap();
    std::fs::create_dir_all(tmp.path().join("shared-hooks")).unwrap();
    let root = root.canonicalize().unwrap();
    let resolved = root.join("../shared-hooks");
    assert!(!is_inside_repo(&lexical_normalize(&resolved), &root, &root.join(".git")));
}

#[test]
fn symlinked_hooks_dir_outside_repo_is_outside() {
    let tmp = tempfile::TempDir::new().unwrap();
    let root = tmp.path().join("repo");
    std::fs::create_dir_all(&root).unwrap();
    std::fs::create_dir_all(tmp.path().join("real-hooks")).unwrap();
    std::os::unix::fs::symlink(tmp.path().join("real-hooks"), root.join("hooks-link")).unwrap();
    let root = root.canonicalize().unwrap();
    let resolved = root.join("hooks-link").canonicalize().unwrap();
    assert!(!is_inside_repo(&resolved, &root, &root.join(".git")));
}

#[test]
fn hooks_dir_inside_repo_is_inside() {
    let tmp = tempfile::TempDir::new().unwrap();
    let root = tmp.path().canonicalize().unwrap();
    std::fs::create_dir_all(root.join("ci-hooks")).unwrap();
    assert!(is_inside_repo(&root.join("ci-hooks"), &root, &root.join(".git")));
}
```

E2E（`tests/cli.rs`; 既存の init 系テストのヘルパー流用）:

```text
relative_global_hooks_path_escaping_repo_is_refused
  - 隔離 HOME の global git config に core.hooksPath=../shared-hooks を設定
  - repo の親に shared-hooks/ を作る
  - portool init → exit 非0、stderr に "outside" / manual hint、shared-hooks/ に post-checkout が無い
command_scoped_hooks_path_is_refused
  - git -c core.hooksPath=<repo外の絶対パス> ... は E2E では PORTOOL 経由で再現困難なので、
    GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.hooksPath GIT_CONFIG_VALUE_0=<abs-outside> を
    env に付けて portool init を起動 → exit 非0、hook 未設置
```

- [ ] **Step 2: テスト失敗を確認**（`cargo test relative_hooks_path` → FAIL: 関数未定義）

- [ ] **Step 3: 実装**

`resolve` を書き換え:

```rust
pub fn resolve(ctx: &GitCtx) -> HooksLocation {
    let configured = gitctx::config_path_value(&ctx.worktree_root, "core.hooksPath");

    if let Some(value) = configured.as_deref().filter(|v| !v.trim().is_empty()) {
        let raw = PathBuf::from(value);
        let resolved = if raw.is_absolute() {
            raw
        } else {
            ctx.worktree_root.join(raw)
        };
        // canonicalize resolves symlinks; a nonexistent dir falls back to a
        // lexical `..` resolution so `../x` cannot dodge the check by not
        // existing yet.
        let canonical = resolved
            .canonicalize()
            .unwrap_or_else(|_| lexical_normalize(&resolved));

        if !is_inside_repo(&canonical, &ctx.worktree_root, &ctx.common_dir) {
            let scope = gitctx::config_scope(&ctx.worktree_root, "core.hooksPath");
            return HooksLocation::SharedScope {
                configured: value.to_string(),
                resolved: canonical,
                scope: scope.unwrap_or_else(|| "unknown".to_string()),
            };
        }
    }

    classify(configured, &ctx.worktree_root, &ctx.common_dir)
}

/// True when `resolved` is inside this repository: under the worktree root
/// or under the git common dir (both already canonical from `GitCtx`).
fn is_inside_repo(resolved: &Path, worktree_root: &Path, common_dir: &Path) -> bool {
    resolved.starts_with(worktree_root) || resolved.starts_with(common_dir)
}

/// Textual `.`/`..` resolution for paths that cannot be canonicalized
/// (target does not exist). Does not touch the filesystem.
fn lexical_normalize(path: &Path) -> PathBuf {
    let mut out = PathBuf::new();
    for c in path.components() {
        match c {
            Component::ParentDir => {
                out.pop();
            }
            Component::CurDir => {}
            other => out.push(other.as_os_str()),
        }
    }
    out
}
```

`SharedScope` の doc コメントと `install_hook` の warning 文言を「repo 外の hooks dir は scope を問わず自動設置しない」に更新。既存 unit test `absolute_existing_dir_is_custom` は classify 直接呼び出しなのでそのまま通る（resolve 経由の拒否は E2E で担保）。既存 E2E の shared-scope テストが「global scope 文言」を assert していたら新文言に合わせて更新。

- [ ] **Step 4: テストが通ることを確認**（`cargo test` 全体）
- [ ] **Step 5: Commit** `fix: refuse core.hooksPath resolving outside the repository (any scope)`

---

### Task 2: hook 書き込み経路の全面改修 — managed block 状態機械・shebang 直後挿入・結果型（指摘2, 3, 6）

**Files:**
- Modify: `src/cmd/init.rs`（install_into: 413-502, deinit_hook: 306-346, replace_managed_block: 528-555, has_managed_block: 517-521, run: 132-146, unhook: 221-227, install_hook: 152-217, deinit_exclude: 350-355, install_managed_hooks: 382-392, remove_hooks: 229-237）
- Test: `src/cmd/init.rs` unit tests + `tests/cli.rs` E2E

**Interfaces:**
- Produces:
  ```rust
  #[derive(Debug, Clone, PartialEq, Eq)]
  pub(crate) enum HookOutcome {
      Installed,
      AlreadyCurrent,
      Removed,
      NotFound,
      SkippedSymlink,
      ManualRequired { reason: String },
  }

  enum ManagedBlockState {
      Absent,
      Valid { begin: usize, end: usize },  // 行 index、inclusive
      Malformed,
  }
  fn managed_block_state(content: &str) -> ManagedBlockState
  fn install_into(hook_path: &Path, script: &str, block: &str) -> Result<HookOutcome>
  fn deinit_hook(hook_path: &Path) -> Result<HookOutcome>
  fn remove_hooks(ctx: &GitCtx) -> Result<Vec<(&'static str, HookOutcome)>>
  ```
- Consumes: Task 1 の `HooksLocation`（変更なし）。

**設計:**
1. **状態機械（指摘2）**: begin/end marker を全行走査。`(1 begin, 1 end, begin < end)` のみ `Valid`。begin 欠落 end のみ・複数 begin/end・順序逆転・end 無しはすべて `Malformed`。`Malformed` のとき install/deinit ともファイルに**一切書き込まず** `ManualRequired` を返す（EOF まで飲み込む旧動作を廃止）。
2. **挿入位置（指摘3）**: foreign shell hook への新規追加は末尾 append をやめ、**shebang 直後**（shebang が無ければ先頭）に block を挿入。既存 Valid block の refresh は現位置で置換（移動しない）。
3. **結果型（指摘6）**: install_into / deinit_hook は上記 enum を返す。
   - install: symlink → `SkippedSymlink`、非 shell → `ManualRequired`、非 UTF-8/読取エラー（NotFound 以外）→ `ManualRequired`（従来の warning は維持）、書いた → `Installed`、既に最新 → `AlreadyCurrent`。
   - deinit: symlink → `SkippedSymlink`、NotFound → `NotFound`、**読取エラー（NotFound 以外）→ `Err`**、Malformed → `ManualRequired`、削除した → `Removed`、portool 由来なし → `NotFound`。
   - full `init` は exclude 更新と sync を実行した後、post-checkout の outcome が `Installed`/`AlreadyCurrent` 以外なら exit 非0（`Error::General` にまとめメッセージ）。`--hook-only` も同じ判定。
   - `unhook` は outcome に応じたメッセージ（`Removed` あり → "removed portool's hooks" / 全て `NotFound` → "no portool hooks found" / `SkippedSymlink` → warning 付き）。読取エラーは `?` で exit 非0。`deinit` も同様に正確な文言。
   - `deinit_exclude`（350-355）の `Err(_) => return Ok(())` を「NotFound のみ Ok、他は Err」へ（init 側 update_exclude: 650-654 と同型に）。

- [ ] **Step 1: 失敗するテストを書く**（unit; 既存 `replace_managed_block_treats_a_truncated_block_as_extending_to_eof` は**削除**して置換）

```rust
#[test]
fn truncated_managed_block_never_modifies_foreign_hook() {
    let content = "#!/bin/sh\n# >>> portool >>>\nportool sync --quiet || true\nimportant-user-code\n";
    assert!(matches!(managed_block_state(content), ManagedBlockState::Malformed));
    // install_into / deinit_hook がファイルを変更しないことを TempDir で確認
    let tmp = TempDir::new().unwrap();
    let hook = tmp.path().join("post-checkout");
    std::fs::write(&hook, content).unwrap();
    let out = install_into(&hook, "SCRIPT", "BLOCK\n").unwrap();
    assert!(matches!(out, HookOutcome::ManualRequired { .. }));
    assert_eq!(std::fs::read_to_string(&hook).unwrap(), content);
    let out = deinit_hook(&hook).unwrap();
    assert!(matches!(out, HookOutcome::ManualRequired { .. }));
    assert_eq!(std::fs::read_to_string(&hook).unwrap(), content);
}

#[test]
fn duplicate_markers_are_malformed() {
    for content in [
        "# >>> portool >>>\nx\n# <<< portool <<<\n# >>> portool >>>\ny\n# <<< portool <<<\n",
        "# <<< portool <<<\n",
        "# <<< portool <<<\n# >>> portool >>>\n",
    ] {
        assert!(matches!(managed_block_state(content), ManagedBlockState::Malformed), "{content:?}");
    }
}

#[test]
fn block_is_inserted_after_shebang_not_appended() {
    let existing = "#!/bin/sh\ndo_something\nexit 0\n";
    let tmp = TempDir::new().unwrap();
    let hook = tmp.path().join("post-checkout");
    std::fs::write(&hook, existing).unwrap();
    let block = "# >>> portool >>>\nB\n# <<< portool <<<\n";
    assert_eq!(install_into(&hook, "S", block).unwrap(), HookOutcome::Installed);
    let written = std::fs::read_to_string(&hook).unwrap();
    assert_eq!(written, "#!/bin/sh\n# >>> portool >>>\nB\n# <<< portool <<<\ndo_something\nexit 0\n");
}

#[test]
fn block_is_inserted_at_top_when_no_shebang() {
    let tmp = TempDir::new().unwrap();
    let hook = tmp.path().join("post-checkout");
    std::fs::write(&hook, "do_something\n").unwrap();
    let block = "# >>> portool >>>\nB\n# <<< portool <<<\n";
    install_into(&hook, "S", block).unwrap();
    assert_eq!(
        std::fs::read_to_string(&hook).unwrap(),
        "# >>> portool >>>\nB\n# <<< portool <<<\ndo_something\n"
    );
}

#[test]
fn install_into_reports_symlink_and_nonshell_as_not_installed() {
    let tmp = TempDir::new().unwrap();
    let hook = tmp.path().join("post-checkout");
    std::os::unix::fs::symlink("/nonexistent", &hook).unwrap();
    assert_eq!(install_into(&hook, "S", "B\n").unwrap(), HookOutcome::SkippedSymlink);
    let py = tmp.path().join("post-merge");
    std::fs::write(&py, "#!/usr/bin/env python3\nprint('hi')\n").unwrap();
    assert!(matches!(install_into(&py, "S", "B\n").unwrap(), HookOutcome::ManualRequired { .. }));
}

#[test]
fn deinit_hook_propagates_read_errors() {
    let tmp = TempDir::new().unwrap();
    let hook = tmp.path().join("post-checkout");
    std::fs::write(&hook, [0xFF, 0xFE]).unwrap(); // non-UTF-8 → read_to_string error
    assert!(deinit_hook(&hook).is_err());
}
```

E2E（tests/cli.rs）:

```text
init_exits_nonzero_when_hook_cannot_be_installed
  - post-checkout を symlink にして portool init → exit 非0（exclude と sync は実施済み: .env.portool は存在する）
unhook_unreadable_hook_exits_nonzero
  - post-checkout を非UTF-8バイトにして portool unhook → exit 非0
existing_hook_exit_before_managed_block_regression
  - "#!/bin/sh\nexit 0\n" の hook に portool init → block が shebang 直後（exit 0 より前）に入り、
    git checkout 相当で sync が走ること（既存 hook 実行系ヘルパーがあれば流用、なければ hook を直接 sh 実行して .env.portool 生成を確認）
```

- [ ] **Step 2: テスト失敗を確認**
- [ ] **Step 3: 実装**（設計どおり。`replace_managed_block` は `Valid { begin, end }` を引数で受ける形に変更。挿入は:）

```rust
/// Inserts `block` right after the shebang line (or at the very top when
/// there is none), so a pre-existing top-level `exit`/`exec` further down
/// cannot make the block unreachable.
fn insert_block_after_shebang(existing: &str, block: &str) -> String {
    let (head, rest) = if existing.starts_with("#!") {
        match existing.find('\n') {
            Some(i) => existing.split_at(i + 1),
            None => (existing, ""),
        }
    } else {
        ("", existing)
    };
    let mut out = String::with_capacity(existing.len() + block.len() + 1);
    out.push_str(head);
    if !head.is_empty() && !head.ends_with('\n') {
        out.push('\n');
    }
    out.push_str(block);
    out.push_str(rest);
    out
}
```

既存 E2E で「block が末尾にある」ことを assert しているテストがあれば挿入位置の新仕様に更新。`migrate_legacy_lines` は現位置置換のまま。Husky 含め挿入規則は統一（portool block は自己完結なので bootstrap 前でも安全）。

- [ ] **Step 4: 全テスト通過を確認**
- [ ] **Step 5: Commit** `fix: fail-closed managed-block parsing, shebang-adjacent insertion, honest hook outcomes`

---

### Task 3: doctor の到達不能 hook 検出（指摘3後半・21）

**Files:**
- Modify: `src/cmd/doctor.rs`（report_hook_health: 218-273）
- Test: `src/cmd/doctor.rs` unit tests + `tests/cli.rs`

**Interfaces:**
- Produces: `fn top_level_exit_precedes(content: &str, before_line: usize) -> bool`（pure）。doctor は managed block の begin 行より前に**行頭から始まる** `exit` / `exit N` / `exec …` があれば advisory warning を出す（exit code は変えない）。

- [ ] **Step 1: 失敗するテストを書く**

```rust
#[test]
fn detects_top_level_exit_before_block() {
    let content = "#!/bin/sh\nexit 0\n# >>> portool >>>\nB\n# <<< portool <<<\n";
    assert!(top_level_exit_precedes(content, 2));
    let indented = "#!/bin/sh\nif x; then\n  exit 1\nfi\n# >>> portool >>>\nB\n# <<< portool <<<\n";
    assert!(!top_level_exit_precedes(indented, 4), "indented exit is not top-level");
    let exec_line = "#!/bin/sh\nexec other-hook \"$@\"\n# >>> portool >>>\nB\n# <<< portool <<<\n";
    assert!(top_level_exit_precedes(exec_line, 2));
}
```

E2E `existing_hook_exit_before_managed_block_is_not_reported_healthy`: 旧形式（末尾 append で exit 後に block がある状態）の hook ファイルを手で作り、`portool doctor` の出力に unreachable warning が含まれることを確認。

- [ ] **Step 2: 失敗確認 → Step 3: 実装**

```rust
fn top_level_exit_precedes(content: &str, before_line: usize) -> bool {
    content.lines().take(before_line).any(|line| {
        if line.starts_with(char::is_whitespace) {
            return false;
        }
        let t = line.trim_end();
        t == "exit" || t.starts_with("exit ") || t == "exec" || t.starts_with("exec ")
    })
}
```

report_hook_health 内、`contains_portool_invocation` 判定の後に managed_block_state（Task 2 の関数を `pub(crate)` にして流用）が `Valid { begin, .. }` のとき `top_level_exit_precedes(&content, begin)` なら
`println!("doctor: warning: {} has a top-level exit/exec before portool's block; the block may never run (re-run 'portool init' to move it)", …)`。あわせて `Malformed` のときも「managed block is malformed; fix it by hand」と報告。

- [ ] **Step 4: 全テスト通過 → Step 5: Commit** `feat: doctor warns when portool's hook block is unreachable or malformed`

---

### Task 4: prune --all の pin 維持（指摘4）

**Files:**
- Modify: `src/cmd/prune.rs`（prune_all の repo 消失分岐: 78-96）
- Test: `tests/cli.rs`

**Interfaces:**
- Consumes: `ProjectEntry.worktrees: BTreeMap<String, WorktreeEntry>`（`pinned: bool`, `block`, `pending_block`）。

- [ ] **Step 1: E2E テストを書く** `prune_all_preserves_pinned_entries_when_common_dir_is_gone`
  - 専用レンジ（例: 19400-19409、着手時に grep で空き確認）で repo を init+sync、`portool pin`
  - repo ディレクトリを `fs::remove_dir_all`
  - `portool prune --all` → exit 0、`ls --json --all` に pinned entry が残る
  - 併せて `prune_all_reclaims_unpinned_when_common_dir_is_gone`: pin なしの同構成では entry ごと消える（既存挙動の回帰確認）
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

repo 消失分岐を per-worktree 評価に変更:

```rust
if !Path::new(key).exists() {
    let project = registry
        .projects
        .get_mut(key)
        .expect("key came from registry.projects.keys()");
    // The repository is gone, but the pin contract still holds: pinned
    // entries are exempt from every GC path, so reclaim only unpinned
    // entries whose ports (block + pending) are confirmed unused, and
    // drop the project entry only once nothing remains.
    let reclaimable: Vec<String> = project
        .worktrees
        .iter()
        .filter(|(_, w)| {
            !w.pinned
                && ports::block_free(w.block)
                && w.pending_block.map(ports::block_free).unwrap_or(true)
        })
        .map(|(path, _)| path.clone())
        .collect();
    let verb = if dry_run { "would prune" } else { "pruned" };
    for path in &reclaimable {
        let block = project.worktrees[path].block;
        println!("{verb} {path} (block {}-{})", block.0, block.1);
        project.worktrees.remove(path);
        changed = true;
    }
    if project.worktrees.is_empty() {
        println!("{verb} project {} ({key})", project.name);
        projects_to_remove.push(key.clone());
        changed = true;
    }
    continue;
}
```

- [ ] **Step 4: 全テスト通過 → Step 5: Commit** `fix: prune --all honors pins even when the repository directory is gone`

---

### Task 5: 内部 git 起動の環境浄化とエラー報告（指摘5）

**Files:**
- Modify: `src/gitctx.rs`（run_git: 146-157, run_git_bytes: 162-173, discover: 33-64, worktree_list_at: 82-93）
- Test: `src/gitctx.rs` unit + `tests/cli.rs`

**Interfaces:**
- Produces:
  ```rust
  const GIT_REPO_ENV_VARS: &[&str] = &[
      "GIT_DIR", "GIT_WORK_TREE", "GIT_COMMON_DIR", "GIT_INDEX_FILE",
      "GIT_OBJECT_DIRECTORY", "GIT_ALTERNATE_OBJECT_DIRECTORIES",
      "GIT_PREFIX", "GIT_IMPLICIT_WORK_TREE", "GIT_SHALLOW_FILE",
      "GIT_GRAFT_FILE",
  ];
  fn git_command(cwd: &Path, args: &[&str]) -> Command   // env_remove 済み
  fn run_git_checked(cwd: &Path, args: &[&str]) -> Result<String>  // spawn失敗/非0/非UTF-8 を区別した Error::General
  ```
- run_git / run_git_bytes（Option 版; 「無いのが正常」な config/symbolic-ref 用）は `git_command` 経由に変更して残す。

- [ ] **Step 1: テストを書く**
  - E2E `git_dir_environment_does_not_override_dash_c_target`: repo A と repo B を作り、A で `GIT_DIR=<B/.git> GIT_WORK_TREE=<B> portool sync` → `ls --json` の path が A を指す（B ではない）。
  - E2E `discover_error_reports_git_stderr`: git repo 外で `portool sync` → stderr に "not inside a git repository" 系の文言（従来文言を維持しつつ git の実エラーを含む）。
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

```rust
fn git_command(cwd: &Path, args: &[&str]) -> Command {
    let mut cmd = Command::new("git");
    cmd.arg("-C").arg(cwd).args(args);
    // A parent git process (hooks!) exports repo-pinning variables; they
    // would make `git -C` operate on the parent's repo instead of `cwd`'s
    // (githooks(5) tells wrappers to clear them).
    for var in GIT_REPO_ENV_VARS {
        cmd.env_remove(var);
    }
    cmd
}

fn run_git_checked(cwd: &Path, args: &[&str]) -> Result<String> {
    let output = git_command(cwd, args)
        .output()
        .map_err(|e| Error::General(format!("failed to run git {}: {e}", args.join(" "))))?;
    if !output.status.success() {
        let stderr = String::from_utf8_lossy(&output.stderr);
        return Err(Error::General(format!(
            "git {} failed ({}): {}",
            args.join(" "),
            output.status,
            stderr.trim()
        )));
    }
    String::from_utf8(output.stdout)
        .map_err(|_| Error::General(format!("git {} produced non-UTF-8 output", args.join(" "))))
}
```

- `discover` は `run_git_checked` を使い、失敗時は従来同様「`{cwd} is not inside a git repository`」を主文に、git の stderr を括弧で併記（既存テストの文言 assert を壊さないよう部分一致を確認して調整）。
- `worktree_list_at` の fallback も `run_git_checked`。
- `run_git`/`run_git_bytes` は本体を `git_command(...).output().ok()?` に置換。

- [ ] **Step 4: 全テスト通過 → Step 5: Commit** `fix: scrub inherited GIT_* repo vars and surface git stderr in errors`

---

### Task 6: doctor の非UTF-8 fail-closed と ID 検証（指摘7）

**Files:**
- Modify: `src/cmd/doctor.rs`（rebuild 経路: 106-143）, `src/envfile.rs`（read_block_from_env: 105-115 の隣に新関数）
- Test: unit + `tests/cli.rs`

**Interfaces:**
- Produces: `envfile::read_identity_from_env(worktree: &Path) -> Option<(String, String)>`（`PORTOOL_PROJECT_ID=` / `PORTOOL_WORKTREE_ID=` 行を parse; どちらか欠けたら None）。
- doctor: worktree パスが `to_str()` 不能なら warning して skip（lossy キー挿入廃止）。env の ID が現在パスから導出した ID と不一致なら warning して skip。

- [ ] **Step 1: テストを書く**
  - unit（envfile）: `read_identity_from_env_parses_both_ids`（.env.portool を TempDir に書いて往復）。
  - E2E `doctor_skips_env_file_with_foreign_identity`: repo を init+sync 後、`.env.portool` を別 worktree からコピーした体（ID 行を書き換え）にして台帳エントリを削除 → `portool doctor` が import せず warning を出す。
  - `doctor_skips_non_utf8_worktree_paths` は macOS では作れないため（APFS が UTF-8 強制）、doctor 内の判定を関数に切り出して unit テスト（`OsStr::from_bytes(b"\xFF")` で PathBuf を作り skip 判定を確認）。
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

doctor.rs:112 の `let wt_key = wt.to_string_lossy().into_owned();` を:

```rust
let wt_key = match wt.to_str() {
    Some(s) => s.to_owned(),
    None => {
        println!(
            "doctor: warning: skipping worktree with non-UTF-8 path: {}",
            wt.to_string_lossy()
        );
        continue;
    }
};
```

block 読み込み（doctor.rs:120 付近）の後に:

```rust
if let Some((env_project_id, env_worktree_id)) = crate::envfile::read_identity_from_env(wt) {
    let expect_p = crate::identity::project_id(&ctx.common_dir);
    let expect_w = crate::identity::worktree_id(&ctx.common_dir, wt);
    if env_project_id != expect_p || env_worktree_id != expect_w {
        println!(
            "doctor: warning: {}/.env.portool identifies a different project/worktree \
             (copied from elsewhere?); not importing its block",
            wt_key
        );
        continue;
    }
}
```

envfile.rs:

```rust
/// The identity pair recorded in a worktree's `.env.portool`, or `None`
/// when the file is missing/unreadable or either variable is absent.
pub fn read_identity_from_env(worktree: &Path) -> Option<(String, String)> {
    let contents = std::fs::read_to_string(worktree.join(".env.portool")).ok()?;
    let mut project = None;
    let mut wt = None;
    for line in contents.lines() {
        if let Some(v) = line.strip_prefix("PORTOOL_PROJECT_ID=") {
            project = Some(v.trim().to_string());
        } else if let Some(v) = line.strip_prefix("PORTOOL_WORKTREE_ID=") {
            wt = Some(v.trim().to_string());
        }
    }
    Some((project?, wt?))
}
```

- [ ] **Step 4: 全テスト通過 → Step 5: Commit** `fix: doctor refuses lossy path keys and verifies env identity before import`

---

### Task 7: deinit の live worktree union（指摘8）

**Files:**
- Modify: `src/cmd/init.rs`（deinit: 243-295）
- Test: `tests/cli.rs`

- [ ] **Step 1: E2E テストを書く** `deinit_without_registry_removes_live_env_files`
  - init+sync で `.env.portool` を作った後、registry.json（と .bak）を削除
  - `portool deinit` → exit 0、`.env.portool` が消えている
  - 併せて `deinit_removes_env_files_missing_from_registry`: linked worktree を追加 sync 後、台帳からその worktree entry だけ手で消して deinit → 両方の env が消える
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

`!keep_allocations` ブロックを:

```rust
if !keep_allocations {
    let _lock = lock::acquire(&paths::lock_path()?, Duration::from_secs(10))?;
    let registry_path = paths::registry_path()?;
    let mut registry_opt = store::load_strict(&registry_path)?;

    // Union of the ledger's recorded paths and the live worktrees git
    // reports: the env files exist on disk regardless of ledger state,
    // so a lost ledger must not leave them behind (README: full deinit
    // removes every worktree's .env.portool).
    let mut env_dirs: std::collections::BTreeSet<PathBuf> =
        ctx.worktree_list()?.into_iter().collect();
    if let Some(registry) = &registry_opt {
        if let Some(project) = registry.find_project(&common_dir_key) {
            env_dirs.extend(project.worktrees.keys().map(PathBuf::from));
        }
    }
    for dir in &env_dirs {
        let env_path = dir.join(".env.portool");
        match fs::remove_file(&env_path) {
            Ok(()) => {}
            Err(err) if err.kind() == std::io::ErrorKind::NotFound => {}
            Err(err) => {
                return Err(Error::General(format!(
                    "failed to remove {}: {err}; no allocations were released",
                    env_path.display()
                )))
            }
        }
    }
    if let Some(registry) = &mut registry_opt {
        if registry.projects.remove(&common_dir_key).is_some() {
            store::save(&registry_path, registry)?;
            println!("portool: released all of this project's allocations");
        }
    }
}
```

- [ ] **Step 4: 全テスト通過 → Step 5: Commit** `fix: deinit removes live worktrees' env files even without a ledger entry`

---

### Task 8: exec の flag 排他（指摘9）

**Files:**
- Modify: `src/main.rs`（Exec の strict: 56-59）
- Test: `tests/exec.rs`（または cli.rs; 既存 exec テストの場所に合わせる）

- [ ] **Step 1: E2E テストを書く** `strict_conflicts_with_reallocate_on_conflict`: `portool exec --strict --reallocate-on-conflict -- true` → exit 64、stderr に conflict 文言。
- [ ] **Step 2: 失敗確認 → Step 3: 実装**: `#[arg(long, conflicts_with = "reallocate_on_conflict")] strict: bool,`
- [ ] **Step 4: 通過確認 → Step 5: Commit** `fix: make exec --strict and --reallocate-on-conflict mutually exclusive`

---

### Task 9: pin label の表示と寿命（指摘10）

**Files:**
- Modify: `src/cmd/ls.rs`（Row: 224-230, print_table: 236-308）, `src/cmd/pin.rs`（set_pinned: 22-48）
- Test: `tests/cli.rs`

**Interfaces:**
- `Row` に `label: String` を追加し、テーブル列は `PROJECT WORKTREE BRANCH BLOCK STATUS LABEL`（末尾追加; STATUS までの既存列順は不変）。label なしは `-`。
- `unpin` は `entry.label = None` にする（label の寿命 = pin の寿命）。

- [ ] **Step 1: E2E テストを書く**
  - `human_ls_displays_pin_label`: pin --label webapp 後の `ls` 出力に `LABEL` ヘッダと `webapp` が含まれる
  - `unpin_clears_label`: pin --label x → unpin → `ls --json` の label が null
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

pin.rs:

```rust
entry.pinned = pinned;
if pinned {
    if let Some(label) = label {
        entry.label = Some(label);
    }
} else {
    // A label's lifetime is the pin's lifetime: unpin clears it so a
    // later label-less pin does not resurrect a stale name.
    entry.label = None;
}
```

ls.rs: Row に label 追加、`rows.push` で `label: crate::display::sanitize(worktree.label.as_deref().unwrap_or("-"))`、header に `LABEL`、`column_width`/`print_row` に列を追加（LABEL は最終列なので pad 不要、status を pad に変更）。

- [ ] **Step 4: 通過確認 → Step 5: Commit** `fix: show pin labels in human ls and clear them on unpin`

---

### Task 10: ls --json の全エラー envelope 化（指摘14）

**Files:**
- Modify: `src/cmd/ls.rs`（run: 62-135）
- Test: `tests/cli.rs`

- [ ] **Step 1: E2E テストを書く** `json_mode_wraps_config_and_git_errors`
  - 壊れた config TOML（`range = "oops"`）で `ls --json` → exit 非0、stdout が `{"format_version":1,"ok":false,"error":...}` として parse できる
  - git repo 外・`--all` なしで `ls --json` → 同様に envelope
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

```rust
pub fn run(json: bool, all: bool) -> Result<()> {
    let result = run_inner(json, all);
    if json {
        if let Err(err) = &result {
            // --json is a machine interface: stdout is JSON on every path,
            // success or failure (P2 #14). Exit code still signals failure.
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "format_version": FORMAT_VERSION,
                    "ok": false,
                    "error": err.to_string(),
                }))
                .expect("error envelope always serializes")
            );
        }
    }
    result
}

fn run_inner(json: bool, all: bool) -> Result<()> { /* 旧 run の中身 */ }
```

旧 run 内の corrupt 分岐にある envelope println（103-113）は**削除**し、`Err` を返すだけにする（二重出力防止）。既存の corrupt-ledger JSON テストが通ることを確認。

- [ ] **Step 4: 通過確認 → Step 5: Commit** `fix: ls --json emits an error envelope for every failure path`

---

### Task 11: backup 鮮度の check 検出（指摘13、doc は Task 16）

**Files:**
- Modify: `src/cmd/check.rs`, `src/store.rs`（backup_path 近辺にヘルパー）
- Test: `tests/cli.rs`

**Interfaces:**
- Produces: `store::backup_is_stale(path: &Path) -> std::io::Result<bool>` — main が存在し、`.bak` が欠落またはバイト不一致なら true。main 欠落時は false。

- [ ] **Step 1: E2E テストを書く** `check_warns_when_backup_is_stale`: init+sync 後、`.bak` を別内容に書き換え → `portool check` は exit 0 のまま stderr に "backup" を含む warning。`.bak` を戻すと warning なし。
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

store.rs:

```rust
/// True when the ledger exists but its `.bak` sibling is missing or does
/// not match byte-for-byte -- i.e. the last backup refresh failed and a
/// `doctor --repair` would restore stale state. Heals on the next save.
pub fn backup_is_stale(path: &Path) -> std::io::Result<bool> {
    let main = match std::fs::read(path) {
        Ok(bytes) => bytes,
        Err(e) if e.kind() == std::io::ErrorKind::NotFound => return Ok(false),
        Err(e) => return Err(e),
    };
    match std::fs::read(backup_path(path)) {
        Ok(bak) => Ok(bak != main),
        Err(e) if e.kind() == std::io::ErrorKind::NotFound => Ok(true),
        Err(e) => Err(e),
    }
}
```

check.rs の ledger 検証成功後:

```rust
let registry_path = paths::registry_path()?;
if store::backup_is_stale(&registry_path).unwrap_or(false) {
    eprintln!(
        "portool: warning: {} is out of date (a backup refresh failed?); \
         it will be refreshed by the next save -- 'doctor --repair' would \
         currently restore stale state",
        store::backup_path(&registry_path).display()
    );
}
```

`backup_path` を `pub` にする（既にそうなら不要）。

- [ ] **Step 4: 通過確認 → Step 5: Commit** `feat: check warns when the ledger backup is stale`

---

### Task 12: 割当不能 config の拒否＋corrupt 退避名の一意化（P3）

**Files:**
- Modify: `src/config.rs`（validation: 74-92）, `src/store.rs`（corrupt_sibling_path: 206-216）
- Test: unit + 既存テスト調整

- [ ] **Step 1: テストを書く**

config.rs unit:

```rust
#[test]
fn config_rejects_block_align_larger_than_range() {
    let err = Config::from_toml_str("range = [3000, 3001]\nblock_align = 5\n").unwrap_err();
    assert!(err.to_string().contains("block_align"), "{err}");
}
```

store.rs unit: `corrupt_sibling_paths_are_unique_within_a_second`（同一 path で2回呼び、異なるファイル名になること — nanos+pid で実質保証されるので、関数を2回呼んで `assert_ne!` は flaky になり得る。代わりに「フォーマットに nanos と pid が含まれる」ことを assert する）。既存テストで `.corrupt-` 名を正規表現/前方一致で見ている箇所を探して更新。

- [ ] **Step 2: 失敗確認 → Step 3: 実装**

config.rs（既存3チェックの後）:

```rust
let span = (self.range.1 as u32) - (self.range.0 as u32) + 1;
if (self.block_align as u32) > span {
    return Err(/* 既存の config error 型に合わせて */ format!(
        "block_align = {} exceeds the port range capacity {} ({}-{}); \
         no block could ever be allocated",
        self.block_align, span, self.range.0, self.range.1
    ));
}
```

store.rs:

```rust
let now = SystemTime::now().duration_since(UNIX_EPOCH).unwrap_or_default();
let pid = std::process::id();
path.with_file_name(format!("{file_name}.corrupt-{}-{pid}", now.as_nanos()))
```

（`Date` 系はテスト決定論に関わらない forensic 名なのでこのままで良い。）

- [ ] **Step 4: 通過確認 → Step 5: Commit** `fix: reject unallocatable configs and make corrupt-ledger aside names unique`

---

### Task 13: sanitize の全コマンド適用＋overlap validation の sort 化（P3）

**Files:**
- Modify: `src/display.rs`, `src/cmd/prune.rs`, `src/cmd/release.rs`, `src/cmd/doctor.rs`, `src/cmd/pin.rs`, `src/cmd/init.rs`（外部由来 path/name を interpolate する println!/eprintln! 全箇所）, `src/registry.rs`（validate の二重ループ: 156-165）
- Test: unit

**Interfaces:**
- Produces: `display::path(p: &Path) -> String`（`sanitize(&p.display().to_string())`）と `display::text(s: &str) -> String`（`sanitize` の別名; 意図を可視化）。以後、人間向け出力で raw の `.display()` / `{path}` を直接使わない。

- [ ] **Step 1: テストを書く**
  - display unit: `path_helper_sanitizes_control_chars`
  - registry unit: 既存 overlap テスト群がそのまま通ること＋新規 `overlap_validation_detects_cross_owner_overlap_after_sort`（3 owner・重複あり/なし混在のケース）
- [ ] **Step 2: 失敗確認 → Step 3: 実装**

registry.rs の validate:

```rust
let mut grouped = owner_grouped_blocks(/* 既存どおり */);
grouped.sort_by_key(|&(_, (start, _))| start);
// Sweep: within one connected run of overlapping intervals all owners must
// match, so a single (owner, max_end) accumulator suffices -- any
// cross-owner overlap surfaces against it. O(n log n) instead of O(n²).
let mut active: Option<(&str, (u16, u16))> = None; // (owner, representative block w/ max end)
for &(owner, block) in &grouped {
    if let Some((a_owner, a_block)) = active {
        if block.0 <= a_block.1 {
            if owner != a_owner {
                return Err(/* 既存の overlap エラー生成をそのまま流用し、a_block と block を渡す */);
            }
            if block.1 > a_block.1 {
                active = Some((a_owner, block));
            }
            continue;
        }
    }
    active = Some((owner, block));
}
```

（owner の型は実コードに合わせる。エラーメッセージ生成は既存二重ループのものを再利用。）

sanitize 適用: `grep -n 'display()\|{path}\|{key}\|{verb} {' src/cmd/*.rs` で外部由来文字列を interpolate する人間向け出力を列挙し、`crate::display::path(...)` / `text(...)` に置換（JSON 出力と `Error::General` 内は対象外のまま。エラーは exit 経路で eprintln されるが、今回は人間向け println!/eprintln! のみを対象とし、エラーメッセージのサニタイズは範囲外とする — レビュー指摘の範囲に合わせる）。

- [ ] **Step 4: 全テスト通過 → Step 5: Commit** `fix: route human output through sanitizers; sort-based overlap validation`

---

### Task 14: ID を 32 hex（128bit）へ拡張（指摘17、破壊的変更）

**Files:**
- Modify: `src/identity.rs`（ID_LEN: 12, doc, 既知ベクトルテスト）, `tests/cli.rs`（ID 長 assert があれば）
- Test: unit

- [ ] **Step 1: テストを更新（失敗させる）**: `ID_LEN` 変更前に、`ids_are_always_16_lowercase_hex_chars` を `ids_are_always_32_lowercase_hex_chars` に改名して 32 を期待させる。既知ベクトルテスト2件は一旦 `todo!` にせず、実行して得た actual で更新する:
  - `cargo test -p portool identity 2>&1` の失敗出力から actual 32-hex を取得し、**独立検証**: `python3 -c "import hashlib; print(hashlib.sha256(b'portool-project-id-v1\0/home/user/dev/myapp/.git').hexdigest()[:32])"` と一致することを確認してからテストに書き込む（worktree_id は `b'portool-worktree-id-v1\0' + common + b'\0' + root`）。
- [ ] **Step 2: 実装**: `const ID_LEN: usize = 32;`、doc コメントの「first 16」→「first 32」更新。
- [ ] **Step 3: 全テスト実行**: env ファイル再生成系・cli.rs で 16桁を前提にした assert（`len() == 16` や既知 ID 文字列）を grep して 32 桁へ更新。
- [ ] **Step 4: Commit** `feat!: widen PORTOOL_PROJECT_ID / PORTOOL_WORKTREE_ID to 32 hex chars (128-bit)`

**注意（CHANGELOG 用に記録）:** 既存 worktree は次回 sync で `.env.portool` の ID が変わる。Docker Compose の project 名等に ID を使っている場合、旧名のコンテナ/ネットワーク/ボリュームが新名で作り直しになる。

---

### Task 15: binary-only 化（指摘12）

**Files:**
- Delete: `src/lib.rs`
- Modify: `src/main.rs`（mod 宣言追加）, `Cargo.toml`（[dev-dependencies] に sha2）, `tests/cli.rs`, `tests/env_paths.rs`
- Test: 全既存テスト

**Interfaces:**
- Produces: crate は bin target のみ。`use portool::…` は今後コンパイル不能（これが SemVer 境界）。
- tests/cli.rs 用ローカルヘルパー（テストファイル内に定義）:
  ```rust
  const HOOK_BLOCK_BEGIN: &str = "# >>> portool >>>";
  const HOOK_OWNED_COMMENT: &str = "# installed by portool";
  fn contains_portool_invocation(content: &str) -> bool {
      content.contains(HOOK_OWNED_COMMENT)
          || content.contains(HOOK_BLOCK_BEGIN)
          || content.contains("portool sync")
  }
  fn overlaps(a: (u16, u16), b: (u16, u16)) -> bool { a.0 <= b.1 && b.0 <= a.1 }
  fn expected_project_id(common_dir: &Path) -> String { truncated32(sha256(b"portool-project-id-v1\0", path_bytes)) }
  fn expected_worktree_id(common_dir: &Path, root: &Path) -> String { /* 同様、\0 区切り */ }
  ```
  （sha2 を使い identity.rs と同じドメイン分離文字列で独立計算。**src からのコピペではなくテスト側で書く** — 期待値の独立性がこのタスクの目的。）

- [ ] **Step 1: src/lib.rs を削除し、main.rs 冒頭に追加**

```rust
mod alloc;
mod cmd;
mod config;
mod display;
mod envfile;
mod envread;
mod error;
mod fault;
mod gc;
mod gitctx;
mod hooks;
mod identity;
mod lock;
mod manifest;
mod paths;
mod ports;
mod registry;
mod store;
```

`use portool::cmd;` → `use crate::cmd;` 相当に修正（`mod cmd;` があるので `use cmd;` 不要、`cmd::init::run` のままパス解決される）。lib.rs のクレート doc（CLI が唯一の安定 IF）は main.rs の `//!` に移設し、「as of v0.9.0 the crate is binary-only」と更新。モジュール間の `pub` は bin クレート内部の可視性としてそのまま（外部には露出しない）。

- [ ] **Step 2: `cargo build` して可視性エラーを潰す**（`pub(crate)` 化は必須ではない。コンパイルが通ればよい）
- [ ] **Step 3: Cargo.toml に `[dev-dependencies] sha2 = "0.10"` を追加し、tests/cli.rs の `portool::…` 参照8箇所を上記ローカルヘルパーに置換**
- [ ] **Step 4: tests/env_paths.rs を CLI black-box に書き換え**: 現在 `portool::config::Config` / `portool::paths` で確認している env var / XDG パス挙動を、隔離環境変数を与えた `portool` バイナリ起動（`ls --json` の effective_config、registry ファイルの実際の生成場所の観察）で同等に検証する。元テストの意図（どの env var がどのパスを制御するか）を1ケースずつ対応させ、カバレッジを落とさない。
- [ ] **Step 5: 全テスト＋clippy 通過 → Commit** `feat!: drop the library target; the CLI is the only interface`

---

### Task 16: MSRV 設定と CI ジョブ（P3）

**Files:**
- Modify: `Cargo.toml`, `.github/workflows/ci.yml`

- [ ] **Step 1: MSRV を実測**: `std::iter::repeat_n`（1.82 安定化）が下限候補。`rustup toolchain install 1.82.0 --profile minimal && cargo +1.82.0 check --all-targets` で確認。エラーが出るならエラー内容の要求バージョンへ引き上げ、通るなら 1.81 以下も試して最小を特定（1.82 で十分なら深追いしない）。
- [ ] **Step 2: `Cargo.toml` の `[package]` に `rust-version = "1.82"`（実測値）を追加**
- [ ] **Step 3: ci.yml に MSRV ジョブを追加**

```yaml
  msrv:
    name: MSRV (1.82)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.82"
      - run: cargo check --all-targets
```

- [ ] **Step 4: ローカルで `cargo +1.82.0 check --all-targets` green → Commit** `chore: declare MSRV and add an MSRV CI job`

---

### Task 17: README / CHANGELOG / version bump（指摘11, 18, 19, backup・ID 文言, リリース準備）

**Files:**
- Modify: `README.md`, `CHANGELOG.md`, `Cargo.toml`（version = "0.9.0"）, `Cargo.lock`（cargo build で更新）

- [ ] **Step 1: README 修正**
  - 冒頭（9行目付近）と 30-32, 618-619: 「real availability is verified at `portool exec`」を「`--check-ports` / `--strict` / `--reallocate-on-conflict` を付けた `exec` で検証できる（デフォルトの exec は検証しない）」に修正
  - 673-680, 694, 28 付近の hook 例を現行生成形（`PORTOOL_BIN="…"` 埋め込み＋PATH fallback、managed block は shebang 直後挿入）に差し替え。実際の生成物と一致させること（`hook_script` / `hook_append_block` の出力をそのまま使う）
  - 644-645: 「Every save also refreshes …」→「Every successful save refreshes … If a backup write fails, portool warns and continues; `portool check` reports a stale backup until the next save heals it.」
  - 274-283: ID を 32 hex / 128-bit truncated SHA-256 に更新し、「unique across the whole machine」→「practically collision-resistant across the whole machine」
  - `--label`（549）: LABEL 列に表示される旨は真になったので保持し、「cleared by `unpin`」を追記
  - exec の flags 説明に `--strict` と `--reallocate-on-conflict` は排他と明記
  - deinit / doctor / hooksPath 拒否（repo 外は scope 不問）の記述を新挙動に同期
- [ ] **Step 2: CHANGELOG.md に 0.9.0 を追加**（Breaking: 32-hex ID / lib target 削除 / repo 外 hooksPath 拒否 / `--strict`+`--reallocate-on-conflict` 排他(usage error 64) / unpin が label を消す / hook 未設置時に init 非0 / malformed block は自動修復せず手動修復要求。Fixed / Added は各タスクの1行サマリ）
- [ ] **Step 3: `Cargo.toml` version = "0.9.0" → `cargo build` で Cargo.lock 更新**
- [ ] **Step 4: Commit** `chore: release prep for v0.9.0 (version, changelog, README)`

---

### Task 18: PR / CI / リリース

- [ ] **Step 1:** `/run-github-actions-locally` でローカル CI green を確認（fmt / clippy -D warnings / 全テスト、Linux 相当）
- [ ] **Step 2:** push、`gh pr create`（base: main）。本文にレビュー対応の対応表（指摘番号 → タスク）を含める。`/pr-complete` で CI 監視・レビュー・修正サイクル
- [ ] **Step 3:** マージ後: `git tag v0.9.0 && git push origin v0.9.0`、`gh release create v0.9.0`（CHANGELOG 抜粋）、`cargo publish`（v0.5.1〜v0.8.0 と同手順）
- [ ] **Step 4:** メモリ `portool-hardening-stack.md` に v0.9.0 の要点と新規使用ポートレンジを追記

---

## Self-Review 結果

- **Spec coverage:** レビュー指摘1〜21＋追加確認事項に対し、指摘1→T1、2→T2、3→T2+T3、4→T4、5→T5、6→T2、7→T6、8→T7、9→T8、10→T9、11/18/19→T17、12→T15、13→T11+T17、14→T10、15(MSRV)→T16、16(config)→T12、17(sanitize)→T13、ID→T14+T17、corrupt名→T12、O(n²)→T13。レビュー提示の回帰テスト13本はすべていずれかのタスクに対応（`doctor_skips_non_utf8_worktree_paths` は macOS 制約により unit 化、`command_scoped_hooks_path_is_refused` は GIT_CONFIG_* env で再現）。
- **型整合:** `HookOutcome` は T2 で定義し T2 内でのみ消費（install_hook/unhook/deinit）。`managed_block_state` は T3 の doctor が `pub(crate)` で参照。`display::path` は T13 定義・T4 の println は T13 で置換される（T4 時点では raw のまま → T13 でまとめて適用、の順で矛盾なし）。
- **Placeholder:** 実装者判断に委ねた箇所は「既存コードの型/エラー生成に合わせる」と明示した2点（registry overlap エラー再利用、config エラー型）のみで、いずれも既存コードの流用指示であり TBD ではない。
