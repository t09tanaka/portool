# portool v0.8.0 Review Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3巡目外部レビューの「リリース前に必須」5件（in-place拡張 / exec警告UX / fsync / 出力サニタイズ / env優先順位）+ fault injection テストを実装し v0.8.0 とする。

**Architecture:** 既存の schema v3 二相更新・fail-closed 設計はそのまま。`store.rs` に durable な `write_atomic` を集約し、`sync.rs` に grow-in-place と移動拒否を追加、`exec.rs` のチェックを opt-in 化、新モジュール `fault.rs`（debug ビルド限定 abort）と `display.rs`（サニタイズ + Unicode 幅）を足す。

**Tech Stack:** Rust, clap, tempfile, serde_json, 新規依存 `unicode-width` のみ追加。

**Spec:** `docs/superpowers/specs/2026-07-17-v0.8.0-review-fixes-design.md`

## Global Constraints

- 依存追加は `unicode-width` のみ。他の crate は追加しない
- fault 機構は `#[cfg(debug_assertions)]` ガード必須（リリースビルドは no-op）
- JSON 出力（`ls --json`）は無加工のまま（serde エスケープに任せる）
- ポートを bind / exact-assert する新テストは専用レンジを使う。本計画での割当:
  Task 3 → `[18900, 18959]`（テストごとに20幅で分割）、Task 4 → `[19000, 19099]`（既存の pin+ephemeral パターンで足りる場合は不要）、Task 5 → `[19200, 19299]`、
  Task 6 → `[19300, 19399]`、Task 7 → `[19100, 19199]`（既存使用済みレンジ:
  3000s/3800s/3900s/4200-4319, 17300-17799, 18000-18499, 18500-18599, 18600-18609, 18700-18799, 18800-18809）
- コミットメッセージ末尾に `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 各タスク完了時に `cargo fmt` + `cargo clippy --all-targets -- -D warnings` + `cargo test` が green であること（静的解析・テスト実行は subagent に委譲してよい）

---

### Task 1: fault.rs — テスト専用クラッシュ注入モジュール

**Files:**
- Create: `src/fault.rs`
- Modify: `src/lib.rs`（モジュール追加）

**Interfaces:**
- Produces: `pub fn fault::point(name: &str)` — 環境変数 `PORTOOL_TEST_FAULT` が `name` に一致したら `process::abort()`（debug ビルドのみ）。Task 2, 3 が呼び出す。

- [ ] **Step 1: 失敗するテストを書く**

`src/fault.rs` を作成:

```rust
//! Test-only crash injection: [`point`] aborts the process when the
//! `PORTOOL_TEST_FAULT` environment variable equals the point's name.
//! Compiled to a no-op outside debug builds, so release binaries carry no
//! fault logic. E2E tests (tests/fault.rs) set the variable on a spawned
//! debug binary to verify crash-consistency at each boundary.

/// Aborts the process if `PORTOOL_TEST_FAULT` equals `name` (debug builds
/// only). Placed at crash-consistency boundaries so E2E tests can verify
/// that an interruption at each point leaves the ledger recoverable.
pub fn point(name: &str) {
    #[cfg(debug_assertions)]
    if std::env::var("PORTOOL_TEST_FAULT").as_deref() == Ok(name) {
        eprintln!("portool: test fault triggered at {name}");
        std::process::abort();
    }
    #[cfg(not(debug_assertions))]
    let _ = name;
}

#[cfg(test)]
mod tests {
    use super::*;

    // The abort path is exercised end-to-end in tests/fault.rs (a spawned
    // binary can die; this test process must not). Here we only verify the
    // no-match paths return normally.
    #[test]
    fn point_returns_when_variable_is_unset_or_different() {
        // The test harness never sets PORTOOL_TEST_FAULT for unit tests.
        point("after_pending_save");
        point("during_backup");
    }
}
```

- [ ] **Step 2: コンパイルが通らないことを確認**

Run: `cargo test --lib fault 2>&1 | head -20`
Expected: FAIL（`lib.rs` に `mod fault` が無いためコンパイルエラー、または unresolved module）

- [ ] **Step 3: lib.rs にモジュールを追加**

`src/lib.rs` の既存モジュール宣言群（すべて `#[doc(hidden)]` が付いている並び）に、アルファベット順の位置へ追加:

```rust
#[doc(hidden)]
pub mod fault;
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test --lib fault`
Expected: PASS (1 test)

- [ ] **Step 5: コミット**

```bash
git add src/fault.rs src/lib.rs
git commit -m "feat: add debug-only fault injection points for crash-consistency tests"
```

---

### Task 2: store.rs — durable write_atomic 集約と atomic バックアップ（P1-3）

**Files:**
- Modify: `src/store.rs`（`write_atomic` 新設、`save` 書き換え）
- Modify: `src/cmd/sync.rs`（ローカル `write_atomic` を削除し `store::write_atomic` を使用）

**Interfaces:**
- Consumes: `fault::point` (Task 1)
- Produces: `pub fn store::write_atomic(path: &Path, contents: &[u8]) -> Result<()>` — temp 書き込み → fsync → rename → 親 dir fsync(best-effort)。Task 3 以降の env 書き込みもこれを使う。
- `store::save` は台帳本体 → `fault::point("after_registry_write")` → バックアップ（temp+fsync+`fault::point("during_backup")`+rename）の順。バックアップ失敗は従来どおり warning。

- [ ] **Step 1: 失敗するテストを書く**

`src/store.rs` の tests モジュールに追加:

```rust
#[test]
fn write_atomic_writes_contents_and_leaves_no_temp_residue() {
    let tmp = TempDir::new().unwrap();
    let path = tmp.path().join("nested").join("file.txt");

    write_atomic(&path, b"hello").unwrap();

    assert_eq!(fs::read(&path).unwrap(), b"hello");
    assert_eq!(
        dir_entries(path.parent().unwrap()),
        vec!["file.txt".to_string()],
        "no temp residue"
    );
}

#[test]
fn save_backup_is_atomic_not_a_plain_copy() {
    // After save, .bak must be byte-identical AND the directory must hold
    // exactly registry.json + registry.json.bak (no .bak.tmp residue).
    let tmp = TempDir::new().unwrap();
    let path = tmp.path().join("registry.json");
    save(&path, &sample_registry()).unwrap();

    let mut entries = dir_entries(tmp.path());
    entries.sort();
    assert_eq!(
        entries,
        vec!["registry.json".to_string(), "registry.json.bak".to_string()]
    );
    assert_eq!(
        fs::read(&path).unwrap(),
        fs::read(backup_path(&path)).unwrap()
    );
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test --lib store 2>&1 | tail -20`
Expected: FAIL（`write_atomic` 未定義のコンパイルエラー）

- [ ] **Step 3: store.rs を実装**

`src/store.rs` の `save` を置き換え、`write_atomic` 群を追加（`use crate::fault;` を追加）:

```rust
/// Saves `registry` to `path` atomically and durably, then refreshes
/// `<path>.bak` (see [`backup_path`]) the same way: a backup failure is a
/// warning on stderr, never a command failure. The backup is written via
/// temp-file + rename too (external review 3rd round P1-3) -- a crash mid-
/// backup can no longer leave a partially overwritten `.bak`.
pub fn save(path: &Path, registry: &Registry) -> Result<()> {
    let json = serde_json::to_string_pretty(registry)?;
    write_atomic_impl(path, json.as_bytes(), None)?;
    fault::point("after_registry_write");

    let bak = backup_path(path);
    if let Err(err) = write_atomic_impl(&bak, json.as_bytes(), Some("during_backup")) {
        eprintln!(
            "portool: warning: failed to update backup {}: {err}",
            bak.display()
        );
    }
    Ok(())
}

/// Writes `contents` to `path` atomically and durably: temp file in the
/// same directory, `sync_all`, rename into place, then a best-effort fsync
/// of the parent directory so the rename itself survives power loss. The
/// parent directory is created if necessary.
pub fn write_atomic(path: &Path, contents: &[u8]) -> Result<()> {
    write_atomic_impl(path, contents, None)
}

fn write_atomic_impl(
    path: &Path,
    contents: &[u8],
    fault_before_rename: Option<&str>,
) -> Result<()> {
    let dir = path.parent().ok_or_else(|| {
        Error::General(format!("{} has no parent directory", path.display()))
    })?;
    fs::create_dir_all(dir)?;
    let mut tmp = tempfile::NamedTempFile::new_in(dir)?;
    tmp.write_all(contents)?;
    tmp.as_file().sync_all()?;
    if let Some(name) = fault_before_rename {
        fault::point(name);
    }
    tmp.persist(path).map_err(|e| Error::from(e.error))?;
    fsync_dir(dir);
    Ok(())
}

/// Best-effort directory fsync after a rename. Failure is a stderr warning,
/// not an error: the rename already succeeded, and some filesystems refuse
/// directory fsyncs.
fn fsync_dir(dir: &Path) {
    if let Ok(handle) = fs::File::open(dir) {
        if let Err(err) = handle.sync_all() {
            eprintln!(
                "portool: warning: failed to fsync directory {}: {err}",
                dir.display()
            );
        }
    }
}
```

- [ ] **Step 4: sync.rs のローカル write_atomic を store へ差し替え**

`src/cmd/sync.rs` 末尾の `fn write_atomic(...)` 定義を削除し、呼び出し2箇所（slow_path の moving 分岐 / 非 moving 分岐）と `reallocate` の1箇所を `store::write_atomic(&ctx.worktree_root.join(".env.portool"), rendered.as_bytes())?;` に変更。`use std::io::Write;` がローカル関数専用だった場合は不要 import も削除。

- [ ] **Step 5: テストが通ることを確認**

Run: `cargo test --lib store && cargo test`
Expected: 全 PASS（既存 `save_leaves_no_temp_file_residue` / `backup_lags_one_save_behind_on_the_next_write` も green のまま）

- [ ] **Step 6: コミット**

```bash
git add src/store.rs src/cmd/sync.rs
git commit -m "fix: make ledger, env, and backup writes durable (fsync + atomic backup)"
```

---

### Task 3: sync.rs — grow-in-place 最優先と移動拒否（P1-1）

**Files:**
- Modify: `src/cmd/sync.rs`（`try_grow_in_place` 新設、grow 分岐書き換え、fault point 設置）
- Test: `src/cmd/sync.rs` tests モジュール + `tests/cli.rs`

**Interfaces:**
- Consumes: `store::write_atomic` (Task 2), `fault::point` (Task 1), `registry::overlaps`, `ports::block_free`
- Produces: `fn try_grow_in_place(registry: &Registry, current: (u16, u16), new_size: u16, pool_end: u16, bind_ok: &mut dyn FnMut((u16, u16)) -> bool) -> Option<(u16, u16)>`（sync.rs 内 private）。grow 成功時は既存 move 機構（pending 二相・generation+1）に乗る。

- [ ] **Step 1: 失敗する unit テストを書く**

`src/cmd/sync.rs` の tests モジュールに追加:

```rust
/// External review 3rd round P1-1: a manifest that outgrows its block must
/// first try extending the current block in place.
#[test]
fn try_grow_in_place_extends_keeping_the_start() {
    let registry = Registry::empty((3000, 3999));
    let mut bind_ok = |_b: (u16, u16)| true;
    assert_eq!(
        try_grow_in_place(&registry, (3000, 3004), 10, 3999, &mut bind_ok),
        Some((3000, 3009))
    );
}

#[test]
fn try_grow_in_place_fails_when_a_neighbor_occupies_the_extension() {
    let mut registry = Registry::empty((3000, 3999));
    registry.reservations.push(Reservation {
        block: (3005, 3009),
        label: None,
        pinned: true,
    });
    let mut bind_ok = |_b: (u16, u16)| true;
    assert_eq!(
        try_grow_in_place(&registry, (3000, 3004), 10, 3999, &mut bind_ok),
        None
    );
}

#[test]
fn try_grow_in_place_fails_when_the_pool_ends() {
    let registry = Registry::empty((3000, 3009));
    let mut bind_ok = |_b: (u16, u16)| true;
    assert_eq!(
        try_grow_in_place(&registry, (3005, 3009), 10, 3009, &mut bind_ok),
        None
    );
}

/// Only the newly added tail is bind-checked: the current block's ports are
/// expected to be in use by this worktree's own processes.
#[test]
fn try_grow_in_place_bind_checks_only_the_added_tail() {
    let registry = Registry::empty((3000, 3999));
    let mut checked = Vec::new();
    let mut bind_ok = |b: (u16, u16)| {
        checked.push(b);
        true
    };
    try_grow_in_place(&registry, (3000, 3004), 10, 3999, &mut bind_ok);
    assert_eq!(checked, vec![(3005, 3009)]);
}

#[test]
fn try_grow_in_place_fails_when_the_tail_is_bound() {
    let registry = Registry::empty((3000, 3999));
    let mut bind_ok = |_b: (u16, u16)| false;
    assert_eq!(
        try_grow_in_place(&registry, (3000, 3004), 10, 3999, &mut bind_ok),
        None
    );
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test --lib cmd::sync 2>&1 | tail -5`
Expected: FAIL（`try_grow_in_place` 未定義）

- [ ] **Step 3: try_grow_in_place を実装し grow 分岐を書き換え**

`src/cmd/sync.rs` に追加:

```rust
/// Tries to widen `current` in place to `new_size` ports, keeping its start
/// (external review 3rd round P1-1: the preferred-slot hash shifts when the
/// block size changes, so the general allocator tends to move a growing
/// worktree even when its own block could simply be extended). The
/// extension must stay inside the pool and clear of every *other* block or
/// reservation; only the newly added tail ports are bind-checked -- the
/// current block's ports are expected to be in use by this worktree's own
/// processes.
fn try_grow_in_place(
    registry: &Registry,
    current: (u16, u16),
    new_size: u16,
    pool_end: u16,
    bind_ok: &mut dyn FnMut((u16, u16)) -> bool,
) -> Option<(u16, u16)> {
    let end = u32::from(current.0) + u32::from(new_size) - 1;
    if end > u32::from(pool_end) {
        return None;
    }
    let candidate = (current.0, end as u16);
    let occupied: Vec<(u16, u16)> = registry
        .all_blocks()
        .into_iter()
        .filter(|&b| b != current)
        .collect();
    if occupied
        .iter()
        .any(|&o| crate::registry::overlaps(o, candidate))
    {
        return None;
    }
    // new_size > current width, so the tail start never overflows past end.
    let tail = (current.1 + 1, candidate.1);
    if !bind_ok(tail) {
        return None;
    }
    Some(candidate)
}
```

`slow_path` の grow 分岐（`Some(entry) if entry.manifest_hash != manifest_state.hash =>` 内の else 側）を書き換え:

```rust
            } else {
                let mut bind_ok = |block: (u16, u16)| ports::block_free(block);
                if let Some(grown) = try_grow_in_place(
                    &registry,
                    entry.block,
                    block_size,
                    config.range.1,
                    &mut bind_ok,
                ) {
                    grown
                } else {
                    // In-place growth is impossible, so satisfying the
                    // manifest means moving. Moving while something is
                    // still listening on the current block would split the
                    // worktree across old and new ports (external review
                    // 3rd round P1-1): refuse and ask for an explicit
                    // action instead.
                    if !ports::block_free(entry.block) {
                        return Err(Error::General(format!(
                            "the manifest now needs {} ports but block {}-{} cannot \
                             grow in place, and something is still listening on it; \
                             stop those processes and re-run 'portool sync', or run \
                             'portool reallocate' to move this worktree explicitly",
                            block_size, entry.block.0, entry.block.1
                        )));
                    }
                    allocate_or_reuse_block(
                        &registry,
                        block_size,
                        config,
                        &common_dir_key,
                        ctx.branch.as_deref(),
                        &worktree_key,
                        Some(entry.block),
                    )?
                }
            }
```

- [ ] **Step 4: fault point を設置**

`slow_path` の moving 分岐と `reallocate` の両方（合計2箇所ずつ）に:

```rust
        entry.pending_block = Some(final_block);   // reallocate では Some(new_block)
        store::save(&registry_path, &registry)?;
        crate::fault::point("after_pending_save");

        crate::fault::point("before_env_write");
        store::write_atomic(&ctx.worktree_root.join(".env.portool"), rendered.as_bytes())?;
        crate::fault::point("after_env_write");
```

- [ ] **Step 5: unit テストが通ることを確認**

Run: `cargo test --lib cmd::sync`
Expected: PASS

- [ ] **Step 6: E2E テストを書く（レンジ 18900-18959 をテストごとに分割）**

割当ブロックの位置はハッシュ依存（temp パスが毎回変わる）で不定なので、既存の `pin_block_to_port` と同様に**台帳を直接書き換えて block を固定**してから grow を起こす。テストごとに専用サブレンジを使い、並行実行時の bind 干渉を避ける:
- grow テスト: `[18900, 18919]`
- blocked-move テスト: `[18920, 18939]`
- refusal テスト: `[18940, 18959]`

`tests/cli.rs` に汎用 pin ヘルパーを追加（既存 `pin_block_to_port` の一般化。`common_dir_key` / `worktree_key` / `init_repo` は既存ヘルパーを使う）:

```rust
/// Rewrites the ledger so `repo`'s worktree block is exactly `block`,
/// making grow/move scenarios deterministic despite hash-based placement.
fn pin_block(env: &TestEnv, repo: &Path, block: (u16, u16)) {
    let mut reg: serde_json::Value =
        serde_json::from_str(&fs::read_to_string(env.registry_path()).unwrap()).unwrap();
    reg["projects"][common_dir_key(repo)]["worktrees"][worktree_key(repo)]["block"] =
        serde_json::json!([block.0, block.1]);
    fs::write(env.registry_path(), serde_json::to_string(&reg).unwrap()).unwrap();
}

/// The (start, end) block of `repo`'s worktree entry in the registry.
fn block_of(env: &TestEnv, repo: &Path) -> (u16, u16) {
    let block = env.registry()["projects"][common_dir_key(repo)]["worktrees"]
        [worktree_key(repo)]["block"]
        .clone();
    (
        block[0].as_u64().unwrap() as u16,
        block[1].as_u64().unwrap() as u16,
    )
}
```

テスト3本:

```rust
/// External review 3rd round P1-1: growing the manifest extends the current
/// block in place instead of hopping to a new hash-preferred slot.
#[test]
fn sync_grows_the_block_in_place_when_the_manifest_expands() {
    let env = TestEnv::new();
    env.write_config("range = [18900, 18919]\n");
    let repo = env.path("grow");
    init_repo(&repo);
    assert!(env.run(&repo, &["sync"]).status.success());
    pin_block(&env, &repo, (18900, 18904));

    // Grow to 10 ports (offset 9) -- must extend in place, keeping 18900.
    fs::write(repo.join(".portool.toml"), "[ports]\nweb = 0\nworker = 9\n").unwrap();
    let out = env.run(&repo, &["sync"]);
    assert!(
        out.status.success(),
        "stderr: {}",
        String::from_utf8_lossy(&out.stderr)
    );
    assert_eq!(
        block_of(&env, &repo),
        (18900, 18909),
        "in-place growth keeps the block start"
    );
}

/// When the extension is blocked by a neighbor and nothing is listening on
/// the current block, sync falls back to a clean move.
#[test]
fn sync_moves_when_growth_is_blocked_and_the_block_is_idle() {
    let env = TestEnv::new();
    env.write_config("range = [18920, 18939]\n");
    let repo = env.path("blocked");
    init_repo(&repo);
    assert!(env.run(&repo, &["sync"]).status.success());
    pin_block(&env, &repo, (18920, 18924));
    // 18925-18929 を予約して in-place 拡張を塞ぐ。
    assert!(env.run(&repo, &["reserve", "18925-18929"]).status.success());

    fs::write(repo.join(".portool.toml"), "[ports]\nweb = 0\nworker = 9\n").unwrap();
    let out = env.run(&repo, &["sync"]);
    assert!(
        out.status.success(),
        "stderr: {}",
        String::from_utf8_lossy(&out.stderr)
    );
    // 10-wide, 5-aligned candidates: 18920 (reservation overlap), 18925
    // (overlap), 18930 -- the only clean landing spot.
    assert_eq!(
        block_of(&env, &repo),
        (18930, 18939),
        "blocked growth falls back to a move"
    );
}

/// Moving with a live listener on the current block is refused with an
/// actionable error (user decision: refuse + explicit action).
#[test]
fn sync_refuses_to_move_while_the_current_block_is_in_use() {
    let env = TestEnv::new();
    env.write_config("range = [18940, 18959]\n");
    let repo = env.path("busy");
    init_repo(&repo);
    assert!(env.run(&repo, &["sync"]).status.success());
    pin_block(&env, &repo, (18940, 18944));
    assert!(env.run(&repo, &["reserve", "18945-18949"]).status.success());
    // 現 block の先頭ポートで listener を立てて「稼働中」を再現。
    let _listener = std::net::TcpListener::bind(("127.0.0.1", 18940)).unwrap();

    fs::write(repo.join(".portool.toml"), "[ports]\nweb = 0\nworker = 9\n").unwrap();
    let out = env.run(&repo, &["sync"]);
    assert!(!out.status.success(), "must refuse the implicit move");
    let stderr = String::from_utf8_lossy(&out.stderr);
    assert!(
        stderr.contains("portool reallocate"),
        "must name the explicit way out: {stderr}"
    );
    assert_eq!(
        block_of(&env, &repo),
        (18940, 18944),
        "the ledger must be unchanged after a refusal"
    );
}
```

（注意: `pin_block` 後は `.env.portool` が台帳とずれるが、slow path が再レンダリングするので問題ない。初回 sync は manifest 無しで行い、pin 後に manifest を書いて hash 差分で grow 分岐に入れる。）

- [ ] **Step 7: E2E テストが通ることを確認**

Run: `cargo test --test cli sync_grows sync_moves sync_refuses`
Expected: PASS（3 tests）

- [ ] **Step 8: 全テスト + コミット**

Run: `cargo test`
Expected: 全 PASS

```bash
git add src/cmd/sync.rs tests/cli.rs
git commit -m "feat: prefer in-place block growth; refuse implicit moves while ports are live"
```

---

### Task 4: exec — デフォルト bind チェック廃止と --check-ports（P1-2）

**Files:**
- Modify: `src/main.rs`（`Exec` に `check_ports` フラグ追加、ヘルプ文更新）
- Modify: `src/cmd/exec.rs`（`run` シグネチャ変更、チェックの opt-in 化）
- Test: `tests/exec.rs`（デフォルト無警告への書き換え + 新フラグのテスト、レンジ 19000-19099）

**Interfaces:**
- Produces: `pub fn exec::run(env_files: &[PathBuf], command: &[OsString], check_ports: bool, strict: bool, reallocate_on_conflict: bool) -> Result<()>`。`strict` / `reallocate_on_conflict` はチェックを含意。

- [ ] **Step 1: 失敗する E2E テストを書く**

`tests/exec.rs` の既存「bind 競合で警告」系テストを新仕様に書き換え + 追加。既存テストの block pin ヘルパー（`pin_block_to_port` 等）と TestEnv の流儀を踏襲する:

```rust
/// v0.8.0: exec no longer bind-checks by default -- running a second
/// process of the same worktree must be silent (external review 3rd round
/// P1-2).
#[test]
fn exec_is_silent_by_default_even_when_block_ports_are_bound() {
    // (TestEnv + repo + sync で block 確定、block 先頭ポートに listener)
    // portool exec -- sh -c 'true' を実行
    // assert: status success && stderr に "in use" を含まない
}

#[test]
fn exec_check_ports_warns_when_block_ports_are_bound() {
    // 同上のセットアップで portool exec --check-ports -- sh -c 'true'
    // assert: status success && stderr が "in use" を含む
}

#[test]
fn exec_strict_fails_on_conflict_without_needing_check_ports() {
    // portool exec --strict -- sh -c 'true'
    // assert: !status.success() && stderr が "in use" を含む
}

#[test]
fn exec_reallocate_on_conflict_moves_and_warns_about_split() {
    // portool exec --reallocate-on-conflict -- sh -c 'true'
    // assert: status success、registry の block が変わっている、
    //         stderr が "split" を含む（稼働中プロセス置き去りの危険警告）
}
```

（各テストの本体は既存 `tests/exec.rs` の同種テストの具体的なセットアップコードをコピーして流用する。レンジは `[19000, 19099]` を `write_config` する。既存テストが 4200-4319 等で同じ挙動を検証している場合は、そのテストを上記新仕様版に**書き換える**こと——旧仕様のテストを残さない。）

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test --test exec 2>&1 | tail -10`
Expected: FAIL（`--check-ports` が未知のフラグ / デフォルト警告が出る）

- [ ] **Step 3: main.rs と exec.rs を実装**

`src/main.rs` の `Exec` variant に追加・修正:

```rust
        /// Warn if the allocated block's ports are already in use (advisory
        /// bind check; off by default -- your own dev servers legitimately
        /// occupy the block).
        #[arg(long)]
        check_ports: bool,
        /// Fail (exit 1) if the allocated block's ports are already in use
        /// (implies --check-ports).
        #[arg(long)]
        strict: bool,
        /// Move to a fresh block if the allocated block's ports are in use
        /// (implies --check-ports). DANGER: processes already running keep
        /// the old ports, so the worktree can end up split across blocks.
        #[arg(long)]
        reallocate_on_conflict: bool,
```

dispatch も `cmd::exec::run(&env_file, &command, check_ports, strict, reallocate_on_conflict)` に変更。

`src/cmd/exec.rs` の `run` のチェック部を置き換え:

```rust
pub fn run(
    env_files: &[PathBuf],
    command: &[OsString],
    check_ports: bool,
    strict: bool,
    reallocate_on_conflict: bool,
) -> Result<()> {
    ...
    let outcome = sync::ensure(&ctx, true)?;

    // v0.8.0 (external review 3rd round P1-2): the bind check is opt-in.
    // A worktree's own dev servers legitimately occupy the block, so a
    // default-on check made every second `portool exec` noisy. --strict
    // and --reallocate-on-conflict imply the check.
    let check = check_ports || strict || reallocate_on_conflict;
    let outcome = if !check || ports::block_free(outcome.block) {
        outcome
    } else if reallocate_on_conflict {
        eprintln!(
            "portool: ports {}-{} are in use; moving to a fresh block -- processes \
             already running keep the old ports, so this worktree may end up split \
             across old and new blocks until they are restarted",
            outcome.block.0, outcome.block.1
        );
        sync::reallocate(&ctx, true)?
    } else {
        eprintln!(
            "portool: ports {}-{} are in use -- this may be this worktree's own \
             running processes (pass --strict to fail instead)",
            outcome.block.0, outcome.block.1
        );
        if strict {
            return Err(Error::General(format!(
                "ports {}-{} are in use and --strict was given",
                outcome.block.0, outcome.block.1
            )));
        }
        outcome
    };
    ...
}
```

モジュール冒頭の doc コメントの該当記述（execution boundary で必ず検査する旨）も opt-in 化に合わせて更新する。

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test --test exec && cargo test`
Expected: 全 PASS

- [ ] **Step 5: コミット**

```bash
git add src/main.rs src/cmd/exec.rs tests/exec.rs
git commit -m "feat!: make exec's bind check opt-in via --check-ports"
```

---

### Task 5: exec --env-file-overrides（env 優先順位オプション）

**Files:**
- Modify: `src/main.rs`（`Exec` に `env_file_overrides` フラグ追加）
- Modify: `src/cmd/exec.rs`（`run` / `compose` にフラグを配線）
- Test: `src/cmd/exec.rs` tests + `tests/exec.rs`（レンジ 19200-19299）

**Interfaces:**
- Produces: `pub fn exec::run(env_files, command, check_ports, strict, reallocate_on_conflict, env_file_overrides: bool)`（Task 4 の5引数にさらに追加、計6引数）。`fn compose(file_entries, parent, portool_vars, env_file_overrides: bool)`。

- [ ] **Step 1: 失敗する unit テストを書く**

`src/cmd/exec.rs` tests に追加（既存 `compose_applies_precedence_order` は第4引数 `false` を渡す形に更新）:

```rust
/// --env-file-overrides: parent < files < portool (external review: an
/// explicitly passed .env.test must beat a stale parent DATABASE_URL).
#[test]
fn compose_with_overrides_lets_files_beat_the_parent() {
    let (map, to_set) = compose(
        vec![vec![("C".into(), file_value("file"))]],
        vec![("C".into(), "parent".into()), ("P".into(), "parent".into())],
        vec![("D".into(), "portool".into())],
        true,
    );
    assert_eq!(raw_of(&map["C"]), "file", "file wins over parent");
    assert_eq!(raw_of(&map["P"]), "parent", "parent-only vars still visible");
    assert_eq!(raw_of(&map["D"]), "portool");
    let names: Vec<&str> = to_set.iter().map(String::as_str).collect();
    assert_eq!(names, ["C", "D"], "parent-only vars are inherited, not re-set");
}

/// Portool variables stay on top regardless of the flag.
#[test]
fn compose_with_overrides_keeps_portool_on_top() {
    let (map, _) = compose(
        vec![vec![("D".into(), file_value("file"))]],
        vec![("D".into(), "parent".into())],
        vec![("D".into(), "portool".into())],
        true,
    );
    assert_eq!(raw_of(&map["D"]), "portool");
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test --lib cmd::exec 2>&1 | tail -5`
Expected: FAIL（compose の引数個数エラー）

- [ ] **Step 3: 実装**

`compose` を書き換え:

```rust
/// Merges the three variable sources into one map. Default precedence
/// (spec §6): earlier env files < later env files < parent environment <
/// portool-managed variables. With `env_file_overrides` (v0.8.0), the
/// parent drops below the files: parent < files < portool, so an
/// explicitly passed env file wins over inherited shell state.
///
/// Also returns the set of names to actually set on the child: file and
/// portool winners only. Parent-sourced winners are excluded — the child
/// inherits them as-is, which keeps non-UTF-8 parent values byte-exact.
fn compose(
    file_entries: Vec<Vec<(String, FileValue)>>,
    parent: Vec<(String, String)>,
    portool_vars: Vec<(String, String)>,
    env_file_overrides: bool,
) -> (BTreeMap<String, Entry>, BTreeSet<String>) {
    let mut map = BTreeMap::new();
    let mut to_set = BTreeSet::new();
    if env_file_overrides {
        for (name, value) in parent {
            map.insert(name, Entry::Literal(value));
        }
        for entries in file_entries {
            for (name, value) in entries {
                to_set.insert(name.clone());
                map.insert(name, Entry::File(value));
            }
        }
    } else {
        for entries in file_entries {
            for (name, value) in entries {
                to_set.insert(name.clone());
                map.insert(name, Entry::File(value));
            }
        }
        for (name, value) in parent {
            to_set.remove(&name);
            map.insert(name, Entry::Literal(value));
        }
    }
    for (name, value) in portool_vars {
        to_set.insert(name.clone());
        map.insert(name, Entry::Literal(value));
    }
    (map, to_set)
}
```

`run` に `env_file_overrides: bool` を追加し `compose(file_entries, parent, portool_vars, env_file_overrides)` へ配線。`src/main.rs` の `Exec` に:

```rust
        /// Let the given env files override inherited environment variables
        /// (default: the parent environment wins over env files).
        #[arg(long)]
        env_file_overrides: bool,
```

- [ ] **Step 4: E2E テストを追加**

`tests/exec.rs`（レンジ `[19200, 19299]` を `write_config`、既存の exec E2E セットアップを流用）:

```rust
#[test]
fn exec_env_file_overrides_beats_the_parent_environment() {
    // .env.test に DATABASE_URL=file-value を書き、spawn する portool コマンドに
    // .env("DATABASE_URL", "parent-value") を設定して
    //   portool exec --env-file-overrides -e .env.test -- sh -c 'echo $DATABASE_URL'
    // assert: stdout == "file-value"
    // 併せてフラグ無しの実行で stdout == "parent-value"（現行仕様の回帰確認）
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cargo test --lib cmd::exec && cargo test --test exec`
Expected: 全 PASS

- [ ] **Step 6: コミット**

```bash
git add src/main.rs src/cmd/exec.rs tests/exec.rs
git commit -m "feat: add exec --env-file-overrides for file-over-parent precedence"
```

---

### Task 6: display.rs — 出力サニタイズと Unicode 幅（P1-4）

**Files:**
- Create: `src/display.rs`
- Modify: `Cargo.toml`（`unicode-width = "0.2"` を `[dependencies]` に追加）
- Modify: `src/lib.rs`（モジュール追加）
- Modify: `src/cmd/ls.rs`（Row 構築時のサニタイズ、幅計算・パディングの表示幅化、予約行のサニタイズ）
- Modify: `src/cmd/sync.rs`（println のパスをサニタイズ）
- Test: `src/display.rs` tests + `tests/cli.rs`

**Interfaces:**
- Produces: `pub fn display::sanitize(s: &str) -> String` / `pub fn display::width(s: &str) -> usize` / `pub fn display::pad(s: &str, w: usize) -> String`

- [ ] **Step 1: 失敗するテストを書く**

`src/display.rs` を作成（テスト込み）:

```rust
//! Human-output hygiene (external review 3rd round P1-4): strings that
//! originate outside portool -- worktree paths, branch names, labels --
//! can legally contain ANSI escapes, newlines, or wide characters. Every
//! human-readable surface routes them through [`sanitize`], and table
//! layout uses display [`width`] instead of byte length. JSON output is
//! untouched: serde escaping already makes it safe.

use unicode_width::UnicodeWidthStr;

/// Returns `s` safe for a terminal: `\n`, `\r`, `\t` become visible
/// two-character escapes, and every other control character (C0 + C1,
/// including ESC -- so no ANSI sequence survives) becomes U+FFFD.
pub fn sanitize(s: &str) -> String {
    let mut out = String::with_capacity(s.len());
    for ch in s.chars() {
        match ch {
            '\n' => out.push_str("\\n"),
            '\r' => out.push_str("\\r"),
            '\t' => out.push_str("\\t"),
            c if c.is_control() => out.push('\u{FFFD}'),
            c => out.push(c),
        }
    }
    out
}

/// The terminal display width of `s` (wide CJK and emoji count as 2).
pub fn width(s: &str) -> usize {
    UnicodeWidthStr::width(s)
}

/// `s` left-justified to display width `w` (space-padded; never truncates).
pub fn pad(s: &str, w: usize) -> String {
    let deficit = w.saturating_sub(width(s));
    let mut out = String::with_capacity(s.len() + deficit);
    out.push_str(s);
    out.extend(std::iter::repeat(' ').take(deficit));
    out
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn sanitize_strips_ansi_escapes() {
        assert_eq!(sanitize("a\u{1b}[31mred"), "a\u{FFFD}[31mred");
    }

    #[test]
    fn sanitize_makes_newlines_visible() {
        assert_eq!(sanitize("a\nb\rc\td"), "a\\nb\\rc\\td");
    }

    #[test]
    fn sanitize_replaces_c1_controls() {
        assert_eq!(sanitize("a\u{9b}b"), "a\u{FFFD}b"); // CSI (C1)
    }

    #[test]
    fn sanitize_keeps_plain_and_wide_text() {
        assert_eq!(sanitize("日本語 path/ブランチ"), "日本語 path/ブランチ");
    }

    #[test]
    fn width_counts_cjk_as_two_columns() {
        assert_eq!(width("日本語"), 6);
        assert_eq!(width("abc"), 3);
    }

    #[test]
    fn pad_uses_display_width() {
        assert_eq!(pad("日本", 6), "日本  ");
        assert_eq!(pad("ab", 4), "ab  ");
        assert_eq!(pad("toolong", 3), "toolong", "never truncates");
    }
}
```

- [ ] **Step 2: 失敗（コンパイルエラー）を確認**

Run: `cargo test --lib display 2>&1 | head -5`
Expected: FAIL（unicode_width 依存なし / モジュール未宣言）

- [ ] **Step 3: 依存とモジュールを追加**

`Cargo.toml` の `[dependencies]` に `unicode-width = "0.2"`。`src/lib.rs` に `#[doc(hidden)] pub mod display;`（アルファベット順の位置）。

Run: `cargo test --lib display`
Expected: PASS

- [ ] **Step 4: ls.rs へ適用**

`src/cmd/ls.rs`:

- Row 構築（`print_table` 内）で全セルをサニタイズ:
  ```rust
  rows.push(Row {
      project: crate::display::sanitize(&project.name),
      worktree: crate::display::sanitize(&shorten_home(path, home.as_deref())),
      branch: crate::display::sanitize(
          worktree.branch.as_deref().unwrap_or("-"),
      ),
      block: format!("{}-{}", worktree.block.0, worktree.block.1),
      status: status.to_string(),
  });
  ```
- `column_width` の `get(r).len()` を `crate::display::width(get(r))` に変更
- `print_row` を `format!("{:<w$}")` から `display::pad` ベースに書き換え:
  ```rust
  fn print_row(row: &Row, w_project: usize, w_worktree: usize, w_branch: usize, w_block: usize) {
      use crate::display::pad;
      println!(
          "{}  {}  {}  {}  {}",
          pad(&row.project, w_project),
          pad(&row.worktree, w_worktree),
          pad(&row.branch, w_branch),
          pad(&row.block, w_block),
          row.status,
      );
  }
  ```
  （最終カラムは従来どおりパディング無し）
- 予約行のラベルをサニタイズ:
  ```rust
  println!(
      "reserved {}-{}  {}",
      r.block.0,
      r.block.1,
      crate::display::sanitize(r.label.as_deref().unwrap_or("-"))
  );
  ```

- [ ] **Step 5: sync.rs の println へ適用**

`slow_path` と `reallocate` の成功時 println を、`ctx.worktree_root.display()` から
`crate::display::sanitize(&ctx.worktree_root.to_string_lossy())` に変更（2箇所）。

- [ ] **Step 6: E2E テストを追加（レンジ 19300-19399）**

`tests/cli.rs`:

```rust
/// External review 3rd round P1-4: a hostile label must not smuggle ANSI
/// escapes or extra lines into `ls` output.
#[test]
fn ls_sanitizes_control_characters_in_labels() {
    let env = TestEnv::new();
    env.write_config("range = [19300, 19399]\n");
    // git repo 作成 + sync（reserve は repo 外でも良いが ls は repo 内で実行）
    let out = env.run(&repo, &["reserve", "19390", "--label", "evil\u{1b}[31m\nINJECTED"]);
    assert!(out.status.success());
    let out = env.run(&repo, &["ls"]);
    assert!(out.status.success());
    let stdout = String::from_utf8_lossy(&out.stdout);
    assert!(!stdout.contains('\u{1b}'), "no raw ESC may survive: {stdout:?}");
    assert!(
        stdout.contains("evil\u{FFFD}[31m\\nINJECTED"),
        "controls must be visibly escaped: {stdout:?}"
    );
}
```

- [ ] **Step 7: 全テスト + コミット**

Run: `cargo test`
Expected: 全 PASS（既存の ls テーブル整形テストがあれば幅計算変更後も green を確認）

```bash
git add Cargo.toml Cargo.lock src/display.rs src/lib.rs src/cmd/ls.rs src/cmd/sync.rs tests/cli.rs
git commit -m "fix: sanitize control characters and use display width in human output"
```

---

### Task 7: fault injection E2E テスト

**Files:**
- Create: `tests/fault.rs`（レンジ 19100-19199）

**Interfaces:**
- Consumes: `fault::point` の5地点（Task 1-3 で設置済み）: `after_pending_save` / `before_env_write` / `after_env_write` / `after_registry_write` / `during_backup`

- [ ] **Step 1: テストファイルを作成**

`tests/fault.rs` を作成。`TestEnv`・`git` ヘルパーは `tests/cli.rs` から必要最小限（`new` / `command` / `run` / `path` / `registry_path` / `write_config` / `registry`、`git` 関数）をコピーし、fault 用に1メソッド追加:

```rust
//! Crash-consistency E2E: PORTOOL_TEST_FAULT aborts the (debug) binary at
//! a specific boundary; a follow-up sync/doctor must recover to a valid,
//! consistent state. Verifies the trust contract the two-phase update and
//! atomic backup are supposed to provide.

impl TestEnv {
    /// Runs portool with a fault point armed.
    fn run_with_fault(&self, dir: &Path, args: &[&str], fault: &str) -> Output {
        self.command()
            .current_dir(dir)
            .env("PORTOOL_TEST_FAULT", fault)
            .args(args)
            .output()
            .expect("failed to spawn portool")
    }
}

/// 共通シナリオ: repo を作って sync → fault を仕込んで reallocate（必ず
/// 二相 move が走る）→ abort を確認 → fault 無しで sync → 完全回復を検証。
fn crash_then_recover(fault: &str) {
    let env = TestEnv::new();
    env.write_config("range = [19100, 19199]\n");
    let repo = env.path("crash");
    // (git repo 作成 + commit、tests/cli.rs の既存パターン)
    let out = env.run(&repo, &["sync"]);
    assert!(out.status.success());
    let before = env.registry();

    let out = env.run_with_fault(&repo, &["reallocate"], fault);
    assert!(
        !out.status.success(),
        "{fault}: the armed binary must abort, got {:?}",
        out.status
    );

    // Recovery: a plain sync must resolve any pending state.
    let out = env.run(&repo, &["sync"]);
    assert!(out.status.success(), "{fault}: recovery sync failed");

    // Invariants after recovery:
    let registry = env.registry();
    // 1. check passes (validate: no overlaps, well-formed).
    let out = env.run(&repo, &["check"]);
    assert!(out.status.success(), "{fault}: check failed after recovery");
    // 2. no pending move survives.
    for project in registry["projects"].as_object().unwrap().values() {
        for entry in project["worktrees"].as_object().unwrap().values() {
            assert!(
                entry["pending_block"].is_null(),
                "{fault}: pending must be resolved"
            );
        }
    }
    // 3. env matches the ledger block.
    let entry_block = /* registry から block [start, end] を取り出す */;
    let env_text = fs::read_to_string(repo.join(".env.portool")).unwrap();
    let header_block = env_text
        .lines()
        .find_map(|l| l.strip_prefix("# block: "))
        .and_then(|rest| rest.split_whitespace().next())
        .expect("env header has a block");
    assert_eq!(
        header_block,
        format!("{}-{}", entry_block.0, entry_block.1),
        "{fault}: env and ledger must agree after recovery"
    );
    // 4. the backup is itself a loadable ledger (atomic backup guarantee).
    let bak = env.registry_path().with_file_name("registry.json.bak");
    let bak_text = fs::read_to_string(&bak).expect("backup exists");
    serde_json::from_str::<serde_json::Value>(&bak_text)
        .expect("backup parses as JSON");
    let _ = before; // silence if unused
}

#[test]
fn recovers_from_a_crash_after_the_pending_save() {
    crash_then_recover("after_pending_save");
}

#[test]
fn recovers_from_a_crash_before_the_env_write() {
    crash_then_recover("before_env_write");
}

#[test]
fn recovers_from_a_crash_after_the_env_write() {
    crash_then_recover("after_env_write");
}

#[test]
fn recovers_from_a_crash_after_the_registry_write() {
    crash_then_recover("after_registry_write");
}

#[test]
fn recovers_from_a_crash_during_the_backup_rename() {
    crash_then_recover("during_backup");
}

/// during_backup 固有: 旧バックアップが部分破壊されないこと。
#[test]
fn a_backup_crash_leaves_the_previous_backup_intact() {
    let env = TestEnv::new();
    env.write_config("range = [19100, 19199]\n");
    let repo = env.path("bak");
    // sync で backup 生成 → その内容を控える
    let out = env.run(&repo, &["sync"]);
    assert!(out.status.success());
    let bak = env.registry_path().with_file_name("registry.json.bak");
    let before = fs::read(&bak).unwrap();
    // during_backup で abort（最初の save = pending save のバックアップで死ぬ）
    let out = env.run_with_fault(&repo, &["reallocate"], "during_backup");
    assert!(!out.status.success());
    assert_eq!(
        fs::read(&bak).unwrap(),
        before,
        "a crash before the backup rename must leave the old backup untouched"
    );
}
```

（`entry_block` の取り出しは Task 3 の `block_of` と同じ手順をこのファイル内に持つ。abort は SIGABRT なので `status.code()` は `None` になり得る——判定は必ず `!status.success()` で行うこと。）

- [ ] **Step 2: テストを実行**

Run: `cargo test --test fault`
Expected: 6 tests PASS（fault 地点が正しく設置されていれば armed run が abort し、recovery が通る）

FAIL する場合は fault point の設置漏れ・名前違いを疑う（Task 1-3 を確認）。

- [ ] **Step 3: 全テスト + コミット**

Run: `cargo test`
Expected: 全 PASS

```bash
git add tests/fault.rs
git commit -m "test: crash-consistency E2E via fault injection at every write boundary"
```

---

### Task 8: リリース準備（version / CHANGELOG / README）

**Files:**
- Modify: `Cargo.toml`（`version = "0.8.0"`）
- Modify: `CHANGELOG.md`（0.8.0 エントリ追加。既存エントリの体裁を踏襲）
- Modify: `README.md`（exec のフラグ説明、grow-in-place と移動拒否の挙動、耐久性の記述）

**Interfaces:** なし（ドキュメントのみ）

- [ ] **Step 1: Cargo.toml のバージョンを 0.8.0 に**

`version = "0.7.0"` → `version = "0.8.0"`。`cargo build` で Cargo.lock も更新。

- [ ] **Step 2: CHANGELOG.md に 0.8.0 エントリを追加**

内容（既存フォーマットに合わせて英語で記載）:
- **Breaking**: `exec` no longer bind-checks the block by default; pass `--check-ports` (or `--strict` / `--reallocate-on-conflict`, which imply it)
- **Breaking**: `sync` refuses an implicit block move while the current block's ports are in use (grow-blocked case); stop the processes or run `portool reallocate`
- Added: in-place block growth is tried first when a manifest outgrows its block (keeps the start, bind-checks only the added tail)
- Added: `exec --env-file-overrides` (files beat the parent environment)
- Fixed: ledger, env, and backup writes are fsynced; the backup is written atomically (temp + rename) instead of an in-place copy
- Fixed: `ls` and sync output sanitize control characters (ANSI escapes cannot survive) and align columns by display width (CJK-safe)

- [ ] **Step 3: README.md を更新**

- `exec` のセクション: デフォルトが無チェックである旨、`--check-ports` / `--strict` / `--reallocate-on-conflict`（危険性明記）/ `--env-file-overrides` の説明
- manifest 拡張の挙動: in-place 優先、塞がっていて稼働中なら拒否＋案内
- 「バックアップ/耐久性」記述があれば「atomic + fsync（macOS の F_FULLFSYNC までは行わない）」に正直化

- [ ] **Step 4: 全テスト + コミット**

Run: `cargo test`
Expected: 全 PASS

```bash
git add Cargo.toml Cargo.lock CHANGELOG.md README.md
git commit -m "chore: release prep for v0.8.0 (version, changelog, README)"
```

---

## 完了後（プラン外・メインセッションで実施）

1. `/run-github-actions-locally` で fmt / clippy / test を green 確認
2. push + `gh pr create`（base: main）→ `/pr-complete` で CI・レビューまで
3. マージ後: tag `v0.8.0`、GitHub Release、`cargo publish`（ユーザー指示があれば）
